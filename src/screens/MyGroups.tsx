import { ArrowLeft, ChevronRight } from 'lucide-react';
import type { ProfileGroup } from '@/screens/Profile';

export function MyGroups({ groups, onBack, onOpenGroup }: { groups: ProfileGroup[]; onBack: () => void; onOpenGroup: (name: string) => void }) {
  return <div className="h-full overflow-y-auto px-4 sm:px-6 pt-6 pb-24 max-w-2xl mx-auto"><button onClick={onBack} className="flex items-center gap-2 text-sevchik-textSecondary mb-8"><ArrowLeft size={20} /> Назад</button><h1 className="font-heading font-extrabold text-2xl mb-5">Все мои группы</h1>{groups.length === 0 ? <p className="text-sm text-sevchik-textSecondary">Вы ещё не состоите в группах</p> : <div className="space-y-3">{groups.map((group) => <button key={group.id} onClick={() => onOpenGroup(group.name)} className="w-full flex items-center gap-3 p-4 bg-[var(--bg-card)] rounded-2xl text-left shadow-[0_8px_24px_rgba(101,70,199,0.08)]"><div className="w-12 h-12 rounded-2xl overflow-hidden shrink-0">{group.avatarUrl ? <img src={group.avatarUrl} alt="" className="w-full h-full object-cover" /> : <div className="w-full h-full bg-sevchik-purple text-white flex items-center justify-center font-heading font-bold">{group.initials || group.name.slice(0, 2).toUpperCase()}</div>}</div><span className="flex-1 font-heading font-bold">{group.name}</span><ChevronRight size={20} className="text-sevchik-textSecondary" /></button>)}</div>}</div>;
}
